#!/usr/bin/env python3
"""
Unified CLI for GEPA experiment analysis.

Usage:
    ./analyze leaderboard [options]
    ./analyze roi [options]
    ./analyze stats [options]
    ./analyze budget [options]

Run './analyze --help' for more information.
"""

import sys

if sys.version_info < (3, 10):
    sys.exit(f"Error: Python 3.10+ requerido (version actual: {sys.version.split()[0]})")

from pathlib import Path

# Add project root to path for imports
project_root = Path(__file__).parent
if str(project_root) not in sys.path:
    sys.path.insert(0, str(project_root))

from shared.analysis.cli import main

if __name__ == "__main__":
    main()
